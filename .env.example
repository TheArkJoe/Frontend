# Google Apps Script / API endpoint that receives lead submissions.
# The endpoint should accept POST fields: email, source, and optional apiKey
VITE_LEADS_ENDPOINT=""

# Optional: sent in request body as apiKey (client-visible in Vite apps)
VITE_LEADS_API_KEY=""
